# Задача: набор спрайтов для игры «Наотмашь»

Это задача **на код, а не на рисование**. Каждый спрайт — рукописный SVG из
простых фигур и путей. Плюс один скрипт, который генерирует четыре текстуры
земли.

Игра: мобильная браузерная аркада, вид сверху, викинг отбивается цепом от
наседающей толпы. Играется одним пальцем.

---

## Почему нужны части тела, а не персонажи

Все тела в игре — физическая симуляция. Герой это три связанные точки (тело,
голова, кисть), враг — три точки (голова, туловище, ноги), оружие — цепочка
звеньев с грузом на конце. Код каждый кадр расставляет части по координатам,
которые посчитала физика.

Отсюда два следствия, которые определяют всю работу:

- Нужны **отдельные части** в нейтральной позе, а не персонажи целиком.
- **Циклы ходьбы, замаха, удара и смерти рисовать не нужно вообще.** Позы
  задаёт симуляция, и нарисованная анимация с ней разъедется.

## Жёсткие ограничения

Нарушение любого делает файл непригодным — игра его не примет.

- **Только SVG.** Никаких `<image>`, внешних ссылок, шрифтов, растровых
  вставок. Игра — один самодостаточный HTML-файл, сборка отвергает всё, что
  тянет что-то извне.
- **Никаких `<filter>`, `<mask>`, размытий и теней.** На экране до тридцати
  объектов одновременно, фильтры убивают частоту кадров на телефоне.
- **Заливка сплошная, обводка одна**, толщиной 6–8% от размера фигуры.
  Градиент допускается только линейный и не больше одного на файл.
- **Каждый файл не больше 1.5 КБ**, весь набор — не больше 120 КБ.
- `viewBox` начинается с `0 0`, размеры ровно как в таблице.
- Каждая фигура развёрнута **вверх** — голова в сторону уменьшения Y.
- Фон прозрачный. **Тени под фигуру не вшивать**: тень рисует код, она
  зависит от позы и вшитая поедет вместе с частью.

## Стиль

Плоские читаемые силуэты, толстый тёмный контур, ограниченная палитра, никакой
мелкой детализации и текстурного шума. Ориентир по контрастности — Downwell и
Nuclear Throne, по подаче вида сверху — Brotato.

Причина: на экране до тридцати врагов, оружие движется со скоростью нескольких
экранов в секунду, ширина экрана телефона 375 точек. Всё, что мельче силуэта,
превращается в кашу. Силуэты разных врагов должны различаться **с одного
взгляда на скорости**, а не при разглядывании.

Текста внутри картинок быть не должно.

---

## Задача 1: спрайты

Имена файлов — ровно такие, размеры в пикселях.

### Герой

| Файл | Размер | Что |
|---|---|---|
| `hero-body.svg` | 60×60 | тело викинга в доспехе, вид сверху |
| `hero-head.svg` | 39×39 | голова в рогатом шлеме |
| `hero-hand.svg` | 18×18 | кисть, сжимающая рукоять |

### Враги — каждый тремя частями: `-head`, `-torso`, `-legs`

| Префикс | Тело целиком | Характер |
|---|---|---|
| `foe-walker-` | ~60 | базовый, поджарый |
| `foe-runner-` | ~48 | тощий, длинноногий, лёгкий |
| `foe-fat-` | ~90 | массивный, приземистый |
| `foe-shield-` | ~66 | плюс отдельный `foe-shield-shield.svg` |
| `boss-hulk-` | ~156 | бронированный, плюс `boss-hulk-plates.svg` |

Пример полного имени: `foe-runner-head.svg`.

Все враги — одна порода нежити или зверья на твой выбор, но узнаваемо разные
по силуэту.

### Оружие — рукоять и боевая часть отдельно

| Файл | Размер |
|---|---|
| `weapon-flail-link.svg` | 30×18, одно звено цепи, стыкуется само с собой встык |
| `weapon-flail-head.svg` | 45×45, шипастый груз |
| `weapon-axe-haft.svg` | 36×18 |
| `weapon-axe-blade.svg` | 66×60 |
| `weapon-sword-haft.svg` | 33×15 |
| `weapon-sword-blade.svg` | 90×33 |

### Предметы

| Файл | Размер | Что |
|---|---|---|
| `prop-rock.svg` | 114×114 | валун, его отбивают оружием |
| `pickup-soul.svg` | 27×27 | подбираемая душа |
| `gib-1.svg` … `gib-6.svg` | 12–42 | ошмётки тел, **одноцветные, без обводки** |

Ошмётки код перекрашивает под цвет врага — поэтому одна заливка, без
градиентов и без контура.

### Иконки

Все 24×24, **одноцветные, `fill="currentColor"`** — код красит их сам.

`icon-longer`, `icon-weight`, `icon-dash`, `icon-spikes`, `icon-sharpen`,
`icon-slick` — карточки прокачки: длиннее оружие, тяжелее наконечник, быстрее
рука, шипы по всему телу, заточка кончика, скользкое тело.

`icon-flail`, `icon-axe`, `icon-sword` — выбор оружия.

`icon-heart`, `icon-gear`, `icon-sound-on`, `icon-sound-off` — интерфейс.

### Палитра

Использовать точно: код красит частицы, брызги и подсветку в эти же цвета, и
рассогласование будет заметно.

```
Ходок          заливка #c2566b   контур #8f3a4c
Бегун                  #dd8b3c          #a4602a
Толстяк                #9a63c4          #6d4590
Щитоносец              #4f93ad          #356878
Туша (босс)            #59626e          #4d5561

Викинг: кожа #d8c39c, доспех #7d5f3d, шлем #98a2ad
Оружие: цеп #96bed7, секира #cdaa73, меч #bec8d4
Душа #8ef2b0, сердце #e0556d
```

### Ещё нужен `sprites.json`

По нему код цепляет части к точкам симуляции. Пивот — доля от размера, точка
крепления к соседней части.

```json
{
  "hero-head": { "w": 39, "h": 39, "pivot": [0.5, 0.6], "note": "низ шеи" },
  "hero-body": { "w": 60, "h": 60, "pivot": [0.5, 0.5], "note": "центр груди" }
}
```

Запись должна быть для каждого файла.

---

## Задача 2: скрипт для текстур земли

Текстуры рукописным SVG не сделать, это шум. Нужен **скрипт** (Python + Pillow,
без обращений к сети), который генерирует четыре бесшовные текстуры 256×256 PNG:

| Файл | Что | Палитра |
|---|---|---|
| `ground-yard.png` | утоптанная земля, мелкие камни | `#12160f` → `#33402a` |
| `ground-ice.png` | замёрзшее озеро, трещины | `#0c131a` → `#2b4d61` |
| `ground-hall.png` | каменные плиты чертога | `#15100c` → `#4a3220` |
| `ground-ash.png` | пепелище | `#121110` → `#3b332c` |

Требования: бесшовность по обеим осям — проверь программно, сравнив
противоположные края. **Тёмные и низкоконтрастные**: разброс яркости внутри
текстуры не больше 12%, никаких ярких пятен, поверх них должен читаться бой.
Каждый файл не больше 25 КБ.

---

## Самопроверка перед сдачей

Приложи вывод:

1. `grep` по всем SVG: нет `<image`, `<filter`, `<mask`, `xlink:href`, `http`
2. размер каждого файла и суммарный по набору
3. каждый SVG парсится — например `xmllint --noout`
4. в `sprites.json` есть запись для каждого файла
5. размеры получившихся PNG и результат проверки бесшовности

## Как отдать

Один архив:

```
sprites/*.svg
sprites.json
tools/make-ground.py
ground/*.png
```

Если что-то не влезает в бюджет 120 КБ — жертвуй с конца: сначала пропы, потом
иконки. Персонажи, враги и оружие нужны целиком.
