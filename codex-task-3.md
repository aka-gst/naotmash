# Большой заказ на графику для «Наотмашь»

Игра: мобильная браузерная аркада, вид сверху, викинг отбивается от наседающей
толпы. Два набора спрайтов уже приняты и стоят в игре — это добор с запасом:
оружие, враги, боссы, герои, иконки, окружение.

Задача **на код**: каждый спрайт — рукописный SVG из простых фигур.

---

## Четыре правила, нарушение которых обесценивает работу

**1. `xmlns` обязателен в каждом файле.** В первом наборе его не было, и это
стоило дороже всего: SVG без `xmlns="http://www.w3.org/2000/svg"` **не
декодируется браузером как картинка**. Файлы выглядели правильными, проходили
все проверки, а в игре молча не появлялись — движок откатывался на запасную
отрисовку, и поломку заметили далеко не сразу. Каждый файл начинается так:

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="W" height="H" viewBox="0 0 W H">
```

**2. Части тела, а не персонажи и не кадры.** Позы задаёт физическая
симуляция: код каждый кадр расставляет части по посчитанным координатам.
Циклы ходьбы, замаха, смерти рисовать не нужно — они не встанут.

**3. Формат манифеста ровно такой.** Ключ — имя файла **без расширения**,
пивот — **доля от размера**, не пиксели:

```json
{ "weapon-spear-blade": { "w": 30, "h": 84, "pivot": [0.5, 0.9] } }
```

**4. Разные силуэты важнее количества.** Первый набор завернули целиком именно
за это: пять врагов оказались одним шестиугольником в пяти размерах. Список
ниже большой, но **если начнёшь повторять форму — остановись и сократи
список**. Двадцать разных фигур полезнее сорока одинаковых.

---

## Стиль

Плоские читаемые силуэты, толстый тёмный контур, ограниченная палитра, никакой
мелкой детализации. Ориентир по контрастности — Downwell и Nuclear Throne, по
подаче вида сверху — Brotato.

Причина жёсткая: на экране до тридцати объектов, оружие движется со скоростью
нескольких экранов в секунду, ширина экрана телефона 375 точек. Опознавать
всё это игрок должен боковым зрением, по контуру, а не разглядывая.

**Сверяйся с уже принятыми файлами** — они лежат в `sprites/`. Новое должно
выглядеть роднёй тому, что в игре: та же толщина обводки, та же условность.

Общие требования: только SVG, без `<image>`, `<filter>`, `<mask>`, внешних
ссылок и шрифтов; сплошная заливка, одна обводка 6–8% от размера; `viewBox`
начинается с `0 0`; фигура развёрнута **вверх** (голова и остриё смотрят в
сторону уменьшения Y); прозрачный фон; теней и свечений не вшивать — их рисует
код. Не больше 1.5 КБ на файл, **весь новый набор — до 250 КБ**.

---

## 1. Оружие

В игре две семьи, и они устроены принципиально по-разному.

**Гибкие** висят на цепи и разгоняются: чем длиннее, тем сильнее бьют и тем
хуже слушаются. Собираются из повторяющегося звена и груза на конце.

**Жёсткие** не болтаются, а продолжают руку. Бьют либо уколом вдоль древка,
либо ударом поперёк.

Каждому оружию — две части: повторяющаяся секция и боевой конец.

### Жёсткие

| Файлы | Размеры | Что это |
|---|---|---|
| `weapon-spear-haft` / `-blade` | 30×16 / 30×84 | копьё: узкий гранёный наконечник, ничего рубящего |
| `weapon-sword-blade` (**замена**) | 34×96 | клинок с крестовиной; нынешний рисовался для гибкого оружия и читается как осколок |
| `weapon-halberd-haft` / `-blade` | 30×16 / 64×88 | алебарда: лезвие плюс крюк, самая сложная форма из всех |
| `weapon-club-haft` / `-blade` | 30×20 / 46×70 | дубина: тупая, тяжёлая, без единой прямой линии |
| `weapon-pick-haft` / `-blade` | 28×14 / 54×62 | клевец: узкий клюв для пробивания брони |

### Гибкие

| Файлы | Размеры | Что это |
|---|---|---|
| `weapon-mace-link` / `-head` | 30×18 / 48×48 | кистень: шипастый шар (у нас уже есть `weapon-flail-*`, этот должен от него явно отличаться — гладкий шар с редкими длинными шипами) |
| `weapon-sickle-link` / `-head` | 26×14 / 62×54 | серп на цепи: широкий полумесяц |
| `weapon-hook-link` / `-head` | 26×14 / 44×66 | гарпун: зазубренный крюк |
| `weapon-hammer-link` / `-head` | 32×20 / 58×52 | молот на цепи: прямоугольный боёк |

Палитра: древки и цепи `#7a5a34` дерево, `#8b9198` металл; боевые части
`#bec8d4` сталь, `#cdaa73` бронза, `#96bed7` вороненая.

---

## 2. Враги

В игре четыре вида, все приняты: ходок, бегун, толстяк, щитоносец. Нужны ещё
пять, **каждый со своим узнаваемым абрисом**. Каждый — три части: `-head`,
`-torso`, `-legs`.

| Префикс | Характер силуэта | Палитра |
|---|---|---|
| `foe-crawler-` | ползун: низкий и широкий, стелется по земле, головы почти не видно | `#6f9a4e` / `#4c6b35` |
| `foe-jumper-` | прыгун: поджатый, на сложенных пружиной длинных ногах, тело маленькое | `#c0a458` / `#8a7436` |
| `foe-brute-` | верзила: непропорционально широкие плечи и крошечная голова, силуэт «Т» | `#a35a3c` / `#6f3a26` |
| `foe-spike-` | колючий: контур утыкан длинными иглами, сам по себе мелкий | `#4a5fa8` / `#33437a` |
| `foe-husk-` | оболочка: тощий, дырявый, с провалом посередине — единственный с пустотой внутри контура | `#3fa08d` / `#2a6f61` |

Размеры: тело целиком у ползуна ~70 в ширину и ~34 в высоту, у прыгуна ~40×62,
у верзилы ~86×54, у колючего ~46×46, у оболочки ~40×58. Части подели сам.

---

## 3. Боссы

Первый босс — «Туша», приземистая бронированная громадина, принята. Нужны два
новых, и оба должны отличаться от неё **всем**, а не размером.

### «Кольчатый» — сегментный червь

Длинный, членистый, ползёт цепочкой. Бить его надо в голову. Собирается из
повторяющегося звена, поэтому частей мало:

| Файл | Размер | Что |
|---|---|---|
| `boss-worm-head` | 76×72 | голова с жвалами, тупая и тяжёлая |
| `boss-worm-link` | 60×54 | одно кольцо тела, повторяется 8–12 раз, стыкуется само с собой |
| `boss-worm-tail` | 48×64 | сужающийся хвост |

Палитра: тело `#7a8a4e`, контур `#4d5a2f`, жвала `#c9d18a`.

### «Костяк» — высокий и тонкий

Противоположность Туше: вытянутый вверх, хрупкий на вид, с длинными руками.

| Файл | Размер |
|---|---|
| `boss-bone-head` | 60×64 |
| `boss-bone-torso` | 84×110 |
| `boss-bone-legs` | 76×86 |
| `boss-bone-arms` | 130×40 (отдельная часть, крепится к плечам) |

Палитра: кость `#d6cfb4`, контур `#7a7358`, провалы `#2b2a24`.

**Проверка силуэтов обязательна**: приложи картинку, где головы всех трёх
боссов и всех девяти врагов нарисованы одним цветом на чёрном и приведены к
одной высоте. Если по ней нельзя различить, кто есть кто, — переделывай.

---

## 4. Герои

Сейчас герой один: тело, голова в рогатом шлеме, кисть. Голова принята и
хороша, менять её не надо. Нужны **три дополнительных облика** — на будущее,
чтобы можно было выбирать бойца.

| Префикс | Кто | Палитра |
|---|---|---|
| `hero-berserk-` | берсерк: голый торс, косматая голова без шлема | кожа `#d8c39c`, волосы `#8a5a2a` |
| `hero-shield-` | дружинник: в кольчуге, шлем с наносником, плечи шире | кольчуга `#8b9198`, плащ `#7a3b3b` |
| `hero-witch-` | ведунья: в плаще с капюшоном, силуэт сужается книзу | плащ `#4a4a6a`, оторочка `#c0a458` |

Каждому — три части: `-body` (60×64), `-head` (42×42), `-hand` (20×20).

Заодно перерисуй `hero-hand` у нынешнего викинга: сейчас это невнятная
загогулина, нужна простая узнаваемая рукавица.

---

## 5. Иконки

Все 24×24, **одноцветные, `fill="currentColor"`** — код красит их сам.
Тринадцать уже есть, нужны ещё.

**Оружие:** `icon-spear`, `icon-halberd`, `icon-club`, `icon-pick`,
`icon-mace`, `icon-sickle`, `icon-hook`, `icon-hammer`.

**Прокачка** (на будущее, механик под них пока нет — рисуй по названию):
`icon-armor` броня, `icon-magnet` притяжение добычи, `icon-heal` лечение,
`icon-crit` критический удар, `icon-slow` замедление врагов, `icon-rage`
ярость, `icon-vampire` вампиризм, `icon-boom` взрыв, `icon-chain` цепная
реакция, `icon-reach` дальность, `icon-thorns` шипы вокруг героя,
`icon-second-wind` второе дыхание.

**Интерфейс:** `icon-pause`, `icon-play`, `icon-retry`, `icon-record` рекорд,
`icon-wave` волна, `icon-skull` смерть, `icon-back`, `icon-share`.

---

## 6. Окружение

В игре четыре арены: двор, лёд, чертог, пепелище. К каждой есть по четыре
пропа, нужны ещё по четыре — `prop-yard-5..8`, `prop-ice-5..8`,
`prop-hall-5..8`, `prop-ash-5..8`, размеры 20–70, с прозрачностью.

И четыре **препятствия**, которые можно бить оружием и в которые можно
швырять врагов, размеры 60–90: `obstacle-barrel`, `obstacle-crate`,
`obstacle-pillar`, `obstacle-brazier`.

---

## 7. Заставка — в последнюю очередь

`splash.png`, портретная, **не больше 80 КБ**: викинг спиной к зрителю посреди
смыкающегося кольца врагов. Просилась дважды и оба раза не приезжала. Если
опять не влезает в бюджет — так и напиши, игра проживёт без неё.

---

## Самопроверка — приложи вывод

1. `grep`: у каждого нового файла есть `xmlns`, ни в одном нет `<image`,
   `<filter`, `<mask`, `xlink:href`;
2. каждый SVG парсится (`xmllint --noout`);
3. размер каждого файла и суммарный по набору;
4. манифест в указанном формате, запись на каждый новый файл;
5. картинка сравнения силуэтов: девять врагов и три босса одним цветом на
   чёрном, приведённые к одной высоте;
6. если что-то из списка не сделал или сократил — напиши прямо, что и почему.

## Как отдать

Архив: `sprites/*.svg`, `sprites.json` только с новыми записями, картинка
проверки силуэтов, `splash.png` если получился.

Если придётся резать ради бюджета — режь с конца: сначала заставка, потом
пропы, потом иконки прокачки. Оружие, враги и боссы нужны целиком.
