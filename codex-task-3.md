# Задача: третий набор спрайтов для «Наотмашь»

Игра: мобильная браузерная аркада, вид сверху, викинг отбивается от толпы.
Два прошлых набора приняты и стоят в игре. Это добор: новое оружие, новый
босс и одна иконка.

Задача **на код**: каждый спрайт — рукописный SVG из простых фигур.

---

## Сначала три правила, нарушение которых убивает работу

**1. `xmlns` обязателен.** В первом наборе его не было, и это стоило дорого:
SVG без `xmlns="http://www.w3.org/2000/svg"` **не декодируется браузером как
картинка**. Файлы выглядели правильными, проходили любую проверку, а в игре
молча не появлялись — движок откатывался на запасную отрисовку, и поломку
заметили не сразу. Каждый файл должен начинаться так:

```xml
<svg xmlns="http://www.w3.org/2000/svg" width="W" height="H" viewBox="0 0 W H">
```

**2. Части тела, а не персонажи и не кадры.** Позы задаёт физическая
симуляция: код каждый кадр расставляет части по посчитанным координатам.
Циклы ходьбы, замаха и смерти рисовать не нужно — они не встанут.

**3. Формат манифеста ровно такой** (во втором наборе он приехал другим, и
его пришлось переводить руками). Ключ — имя файла **без расширения**, пивот —
**доля от размера**, не пиксели:

```json
{
  "weapon-spear-blade": { "w": 30, "h": 84, "pivot": [0.5, 0.9] }
}
```

---

## Что нарисовать

Размеры в пикселях, отрисовка идёт с запасом ×3 под плотные экраны.

### 1. Копьё — новое оружие

Сейчас оно в игре с чужим спрайтом. Копьё **жёсткое**: не болтается на цепи,
а продолжает руку и бьёт уколом от себя.

| Файл | Размер | Что |
|---|---|---|
| `weapon-spear-haft.svg` | 30×16 | секция древка, повторяется вдоль, стыкуется сама с собой встык по длинной стороне |
| `weapon-spear-blade.svg` | 30×84 | узкий гранёный наконечник; длинный и явно колющий, ничего рубящего |

Древко `#7a5a34`, наконечник `#c8cdd6`.

### 2. Меч — заменить лезвие

`weapon-sword-blade.svg` рисовалось для гибкого оружия и читается как осколок.
Меч стал жёстким, им рубят поперёк — нужен узнаваемый клинок с крестовиной и
намёком на рукоять.

| Файл | Размер |
|---|---|
| `weapon-sword-blade.svg` | 34×96 |

Клинок `#bec8d4`, крестовина и навершие `#8a6238`.

### 3. Второй босс — «Кольчатый», сегментный червь

Первый босс «Туша» — приземистая бронированная громадина. Второй должен
отличаться от неё всем: он **длинный и членистый**, ползёт цепочкой звеньев,
и бить его надо в голову.

Собирается из повторяющегося звена, поэтому частей мало:

| Файл | Размер | Что |
|---|---|---|
| `boss-worm-head.svg` | 76×72 | голова с жвалами, тупая и тяжёлая |
| `boss-worm-link.svg` | 60×54 | одно кольцо тела; стыкуется само с собой, повторяется 8–12 раз |
| `boss-worm-tail.svg` | 48×64 | сужающийся хвост |

Палитра: тело `#7a8a4e`, контур `#4d5a2f`, жвала и хитин `#c9d18a`.

**Проверка силуэта обязательна.** Приложи картинку: голова червя и голова
первого босса (`boss-hulk-head.svg`) рядом, одним цветом на чёрном,
приведённые к одной высоте. Если непонятно, кто из них кто, — переделывай.
В прошлый раз ровно на этом набор и завернули: пять врагов оказались одним
шестиугольником в пяти размерах.

### 4. Иконка

`icon-spear.svg`, 24×24, одноцветная, `fill="currentColor"` — как остальные
иконки в наборе.

### 5. Заставка — если останется время

`splash.png`, портретная, **не больше 80 КБ**: викинг спиной к зрителю посреди
смыкающегося кольца врагов. Просилась дважды и оба раза не приезжала; если
опять не влезает — так и напиши, игра проживёт.

---

## Остальные требования — прежние

- только SVG, без `<image>`, `<filter>`, `<mask>`, внешних ссылок и шрифтов;
- сплошная заливка, одна обводка 6–8% от размера, не больше 1.5 КБ на файл;
- `viewBox` начинается с `0 0`, фигура развёрнута **вверх** (остриё и голова
  смотрят в сторону уменьшения Y);
- прозрачный фон, теней и свечений в спрайт не вшивать — их рисует код;
- плоские читаемые формы, никакой мелкой детализации: на экране до тридцати
  объектов, и всё быстро движется.

**Стиль сверяй с уже принятыми файлами** — они лежат в той же папке
`sprites/`. Новое должно выглядеть роднёй тому, что уже в игре: та же
толщина обводки, та же степень условности.

Палитра остального набора, для согласования:

```
Ходок #c2566b / #8f3a4c     Бегун #dd8b3c / #a4602a
Толстяк #9a63c4 / #6d4590   Щитоносец #4f93ad / #356878
Туша #59626e / #4d5561
Викинг: кожа #d8c39c, доспех #7d5f3d, шлем #98a2ad
Цеп #96bed7, секира #cdaa73, меч #bec8d4
```

## Самопроверка — приложи вывод

1. `grep` по всем новым файлам: у каждого есть `xmlns`, ни в одном нет
   `<image`, `<filter`, `<mask>`;
2. каждый SVG парсится (`xmllint --noout`);
3. размер каждого файла;
4. манифест в указанном формате, запись на каждый новый файл;
5. картинка сравнения силуэтов двух боссов.

## Как отдать

Архив: новые `*.svg` в корне или в `sprites/`, `sprites.json` только с новыми
записями, картинка проверки силуэтов, `splash.png` если получился.
